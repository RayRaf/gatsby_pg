# ✅ Чек-лист миграции на Docker PostgreSQL

## 📋 Перед началом

- [ ] Docker установлен и работает
- [ ] Node.js установлен (версия 18+)
- [ ] Прочитан файл `MIGRATION_SUMMARY.md`
- [ ] Создана резервная копия проекта (опционально)

---

## 🚀 Шаг 1: Установка зависимостей (5 минут)

### 1.1 Установить PostgreSQL пакеты

```bash
npm install --legacy-peer-deps pg @types/pg
```

**Ожидаемый результат:**
```
added 2 packages, and audited 231 packages in 3s
```

- [ ] Пакеты установлены успешно

### 1.2 Создать .env файл

```bash
cp .env.example .env
```

- [ ] Файл `.env` создан

---

## 📝 Шаг 2: Обновление кода (30 минут)

### 2.1 Обновить app/page.tsx

Откройте файл `CODE_CHANGES.md` и примените изменения:

**Функции для обновления:**
- [ ] `handleSave` - обновление существующей регистрации (строка ~141)
- [ ] `handleSave` - создание новой регистрации (строка ~156)
- [ ] `handleDelete` (строка ~179)
- [ ] `checkExistingRegistration` (строка ~214)
- [ ] Удалить импорт `createClient` из `@/lib/supabase/client`

**Команда для быстрого поиска:**
```bash
grep -n "createClient\|supabase" app/page.tsx
```

### 2.2 Обновить app/results/page.tsx

**Функции для обновления:**
- [ ] `fetchParticipants` (строка ~27)
- [ ] Удалить импорт `createClient` из `@/lib/supabase/client`

**Команда для быстрого поиска:**
```bash
grep -n "createClient\|supabase" app/results/page.tsx
```

### 2.3 Обновить .gitignore (опционально)

Добавьте в конец файла:
```gitignore
# Docker
.env
docker-compose.override.yml

# PostgreSQL data
postgres-data/
```

- [ ] `.gitignore` обновлен

---

## 🐳 Шаг 3: Запуск Docker (10 минут)

### 3.1 Запустить Docker Compose

```bash
docker-compose up -d
```

**Ожидаемый результат:**
```
[+] Running 3/3
 ✔ Network v0-gatsby-style-website_default    Created
 ✔ Container gatsby-postgres                  Started
 ✔ Container gatsby-app                       Started
```

- [ ] Контейнеры запущены

### 3.2 Проверить статус

```bash
docker-compose ps
```

**Ожидаемый результат:**
```
NAME              STATUS        PORTS
gatsby-postgres   Up (healthy)  0.0.0.0:5432->5432/tcp
gatsby-app        Up            0.0.0.0:3000->3000/tcp
```

- [ ] Оба контейнера в статусе "Up"

### 3.3 Проверить логи PostgreSQL

```bash
docker-compose logs postgres | tail -20
```

**Искать строки:**
```
PostgreSQL init process complete; ready for start up.
database system is ready to accept connections
```

- [ ] PostgreSQL запустился успешно

### 3.4 Проверить логи приложения

```bash
docker-compose logs app | tail -20
```

**Искать строки:**
```
- Local:        http://localhost:3000
✓ Ready in 2.5s
```

- [ ] Next.js запустился успешно

---

## 🧪 Шаг 4: Тестирование (15 минут)

### 4.1 Открыть приложение

```bash
# Открыть в браузере
open http://localhost:3000
# Или просто перейти по ссылке: http://localhost:3000
```

- [ ] Приложение открывается

### 4.2 Тест: Создание регистрации

1. Нажать кнопку "Подтвердить участие"
2. Заполнить форму:
   - Имя: "Тестовый Пользователь"
   - Выбрать напитки: Шампанское, Вино
   - Пожелания: "Это тестовая регистрация"
3. Нажать "Сохранить"

- [ ] Регистрация создана без ошибок
- [ ] Модальное окно закрылось

### 4.3 Тест: Просмотр регистраций

1. Перейти на страницу результатов: http://localhost:3000/results
2. Проверить, что отображается созданная регистрация

- [ ] Регистрация отображается в списке
- [ ] Имя, напитки и пожелания корректны

### 4.4 Тест: Обновление регистрации

1. Вернуться на главную страницу
2. Снова нажать "Подтвердить участие"
3. Форма должна быть заполнена данными
4. Изменить выбор напитков
5. Сохранить

- [ ] Данные загрузились из БД
- [ ] Обновление прошло успешно
- [ ] Изменения отображаются в /results

### 4.5 Тест: Удаление регистрации

1. Открыть форму подтверждения
2. Нажать "Удалить регистрацию"
3. Подтвердить удаление

- [ ] Регистрация удалена
- [ ] Cookie очищен
- [ ] Регистрация не отображается в /results

### 4.6 Тест: Проверка данных в БД

```bash
docker-compose exec postgres psql -U gatsby_user -d gatsby_db -c "SELECT id, name, drinks FROM registrations;"
```

**Ожидаемый результат:**
```
 id | name | drinks
----+------+--------
(0 rows)
```

- [ ] Данные в БД соответствуют ожиданиям

---

## 🎯 Шаг 5: Финальные проверки (5 минут)

### 5.1 Проверить подключение к PostgreSQL

```bash
docker-compose exec postgres psql -U gatsby_user -d gatsby_db -c "\dt"
```

**Ожидаемая таблица:**
```
              List of relations
 Schema |      Name       | Type  |   Owner
--------+-----------------+-------+-----------
 public | registrations   | table | gatsby_user
```

- [ ] Таблица `registrations` существует

### 5.2 Проверить структуру таблицы

```bash
docker-compose exec postgres psql -U gatsby_user -d gatsby_db -c "\d registrations"
```

**Ожидаемые колонки:**
- id (uuid)
- name (text)
- drinks (text[])
- individual_wishes (text)
- cookie_id (text)
- created_at (timestamp)
- updated_at (timestamp)

- [ ] Структура таблицы корректна

### 5.3 Проверить триггер обновления времени

```bash
docker-compose exec postgres psql -U gatsby_user -d gatsby_db -c "\d+ registrations" | grep -i trigger
```

**Должен быть триггер:** `update_registrations_updated_at`

- [ ] Триггер создан

### 5.4 Проверить логи на ошибки

```bash
docker-compose logs | grep -i error | tail -20
```

- [ ] Нет критических ошибок

---

## 📊 Шаг 6: Performance тест (опционально)

### 6.1 Создать несколько регистраций

Создайте 3-5 тестовых регистраций через UI

- [ ] Все регистрации созданы успешно

### 6.2 Проверить скорость загрузки

1. Откройте DevTools (F12)
2. Перейдите на /results
3. Проверьте время загрузки API запроса

**Ожидается:** < 100ms для запроса `/api/registrations`

- [ ] API отвечает быстро

### 6.3 Проверить connection pool

```bash
docker-compose logs app | grep "Executed query"
```

**Должны быть логи типа:**
```
Executed query { text: 'SELECT * FROM registrations...', duration: 15, rows: 5 }
```

- [ ] Query логи работают
- [ ] Duration разумное (< 50ms)

---

## 🧹 Шаг 7: Очистка (после тестов)

### 7.1 Удалить тестовые данные

```bash
docker-compose exec postgres psql -U gatsby_user -d gatsby_db -c "DELETE FROM registrations WHERE name LIKE '%Тест%';"
```

- [ ] Тестовые данные удалены

### 7.2 Проверить размер БД

```bash
docker-compose exec postgres psql -U gatsby_user -d gatsby_db -c "SELECT pg_size_pretty(pg_database_size('gatsby_db'));"
```

- [ ] Размер БД разумный

---

## 📈 Итоговый статус

### Обязательные шаги (✅ = выполнено)

#### Установка
- [ ] Установлены зависимости (pg, @types/pg)
- [ ] Создан .env файл

#### Код
- [ ] Обновлен app/page.tsx (4 функции)
- [ ] Обновлен app/results/page.tsx (1 функция)

#### Docker
- [ ] Docker Compose запущен
- [ ] PostgreSQL работает
- [ ] Next.js приложение работает

#### Тестирование
- [ ] Создание регистрации
- [ ] Просмотр регистраций
- [ ] Обновление регистрации
- [ ] Удаление регистрации
- [ ] Проверка данных в БД

### Опциональные шаги

- [ ] Обновлен .gitignore
- [ ] Performance тест выполнен
- [ ] Тестовые данные очищены
- [ ] Документация прочитана

---

## 🎉 Поздравляю!

Если все пункты отмечены ✅, миграция завершена успешно!

### Следующие шаги:

1. **Production готовность:**
   - [ ] Изменить пароли в docker-compose.yml
   - [ ] Настроить SSL/TLS
   - [ ] Добавить резервное копирование БД
   - [ ] Настроить мониторинг

2. **Удалить старый код (опционально):**
   ```bash
   rm -rf lib/supabase
   npm uninstall @supabase/ssr @supabase/supabase-js
   ```

3. **Commit changes:**
   ```bash
   git add .
   git commit -m "feat: migrate from Supabase to Docker PostgreSQL"
   ```

---

## 🆘 Проблемы?

### PostgreSQL не запускается
```bash
docker-compose down -v
docker-compose up -d postgres
docker-compose logs postgres
```

### Next.js не запускается
```bash
docker-compose restart app
docker-compose logs app
```

### Ошибки подключения к БД
```bash
# Проверить, что PostgreSQL готов
docker-compose exec postgres pg_isready -U gatsby_user -d gatsby_db
```

### Полный перезапуск
```bash
docker-compose down -v
docker-compose up -d
```

---

## 📞 Документация

- **Быстрый старт:** [DOCKER_QUICKSTART.md](./DOCKER_QUICKSTART.md)
- **Полное руководство:** [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md)
- **Изменения в коде:** [CODE_CHANGES.md](./CODE_CHANGES.md)
- **Сводка:** [MIGRATION_SUMMARY.md](./MIGRATION_SUMMARY.md)

---

**Удачи! 🚀**
