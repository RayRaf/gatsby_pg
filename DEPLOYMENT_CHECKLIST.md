# ✅ Чеклист применения исправлений

## Предварительная проверка
- [ ] SSH-доступ к серверу работает
- [ ] Docker и docker-compose установлены
- [ ] Контейнеры `gatsby-app-prod` и `gatsby-postgres-prod` запущены
- [ ] Есть актуальный бэкап БД (`backup_2025-11-01_09-25-33.sql`)

## Шаги применения (выберите один способ)

### 🚀 Способ 1: Автоматический (рекомендуется)

На сервере:
```bash
cd ~/gatsby_pg
bash deploy-fix.sh
```

- [ ] Скрипт выполнен успешно
- [ ] Проверено: записей в БД = записям через API
- [ ] Открыта страница `/results` и отображаются участники

---

### 🔧 Способ 2: Вручную

#### Шаг 1: Загрузить изменения
```bash
cd ~/gatsby_pg
git pull origin main  # если используете git
# ИЛИ загрузите изменённые файлы вручную
```
- [ ] Изменения загружены

#### Шаг 2: Проверить изменённые файлы
```bash
grep "public.registrations" lib/db/registrations.ts
grep "search_path=public" lib/db/postgres.ts
```
- [ ] В `registrations.ts` есть `public.registrations`
- [ ] В `postgres.ts` есть `search_path=public`

#### Шаг 3: Остановить приложение
```bash
docker-compose -f docker-compose.prod.yml stop app
```
- [ ] Контейнер остановлен: `docker ps` не показывает `gatsby-app-prod`

#### Шаг 4: Пересобрать образ
```bash
docker-compose -f docker-compose.prod.yml build app
```
- [ ] Образ собран без ошибок

#### Шаг 5: Запустить приложение
```bash
docker-compose -f docker-compose.prod.yml up -d app
```
- [ ] Контейнер запущен: `docker ps` показывает `gatsby-app-prod`

#### Шаг 6: Проверить логи
```bash
docker logs gatsby-app-prod --tail=50
```
- [ ] Нет ошибок подключения к БД
- [ ] Видны сообщения: "DATABASE_URL exists: true"

---

## Проверка результатов

### 1. Проверка базы данных
```bash
docker exec -it gatsby-postgres-prod psql -U gatsby_user -d gatsby_db -c "SELECT COUNT(*) FROM public.registrations;"
```
**Ожидается:** 22

- [ ] ✅ Результат: _____ (впишите число)

### 2. Проверка API
```bash
curl http://localhost:3000/api/registrations | jq '. | length'
# ИЛИ если нет jq:
docker exec -it gatsby-app-prod curl -s http://localhost:3000/api/registrations
```
**Ожидается:** Массив с 22 объектами

- [ ] ✅ Результат: _____ (впишите число)

### 3. Проверка веб-интерфейса
Откройте в браузере: `http://your-domain.com/results`

- [ ] ✅ Отображается 22 участника
- [ ] ✅ Видны имена, напитки и пожелания
- [ ] ✅ Внизу показано "Всего зарегистрировано: 22 участников"

### 4. Диагностический скрипт
```bash
docker exec -it gatsby-app-prod node scripts/check-db-connection.js
```

- [ ] ✅ Показывает "Connected successfully"
- [ ] ✅ Показывает "Total registrations: 22"
- [ ] ✅ Показывает примеры записей

---

## Если что-то пошло не так

### Проблема: Контейнер не запускается
```bash
docker logs gatsby-app-prod
```
Ищите ошибки. Возможные причины:
- Ошибка синтаксиса в коде
- Проблема с подключением к БД
- Неправильная переменная окружения

**Действие:**
```bash
# Откатить изменения
docker-compose -f docker-compose.prod.yml down app
# Восстановить старый образ или исправить ошибку
```

- [ ] Проблема решена

---

### Проблема: API возвращает пустой массив
```bash
# Проверить search_path
docker exec -it gatsby-postgres-prod psql -U gatsby_user -d gatsby_db -c "SHOW search_path;"

# Проверить таблицы
docker exec -it gatsby-postgres-prod psql -U gatsby_user -d gatsby_db -c "\dt public.*"

# Проверить данные
docker exec -it gatsby-postgres-prod psql -U gatsby_user -d gatsby_db -c "SELECT COUNT(*) FROM public.registrations;"
```

**Действие:**
1. Убедитесь, что в файле `lib/db/registrations.ts` все запросы используют `public.registrations`
2. Убедитесь, что в `lib/db/postgres.ts` есть опция `search_path=public`
3. Пересоберите и перезапустите

- [ ] Проблема решена

---

### Проблема: В БД нет данных (COUNT = 0)
```bash
# Восстановить из бэкапа
docker exec -i gatsby-postgres-prod psql -U gatsby_user -d gatsby_db < ~/backup_2025-11-01_09-25-33.sql

# Проверить
docker exec -it gatsby-postgres-prod psql -U gatsby_user -d gatsby_db -c "SELECT COUNT(*) FROM public.registrations;"
```

- [ ] Данные восстановлены

---

## Финальная проверка

### Всё работает, если:
- [ ] ✅ БД содержит 22 записи
- [ ] ✅ API возвращает 22 записи
- [ ] ✅ Веб-интерфейс показывает 22 участника
- [ ] ✅ Нет ошибок в логах приложения
- [ ] ✅ Нет ошибок в логах БД

### Документация
- [ ] Создан новый бэкап после успешного применения исправлений
- [ ] Отмечено время применения исправлений: __________
- [ ] Записана версия Docker-образа: __________

---

## Команды для бэкапа (после успешного применения)

```bash
# Создать бэкап
docker exec -i gatsby-postgres-prod pg_dump -U gatsby_user gatsby_db > ~/backup_after_fix_$(date +%Y%m%d_%H%M%S).sql

# Проверить размер
ls -lh ~/backup_after_fix_*.sql
```

- [ ] Бэкап создан: _______________ (имя файла)

---

## Полезные команды для мониторинга

```bash
# Логи в реальном времени
docker logs gatsby-app-prod -f

# Статус контейнеров
docker ps

# Использование ресурсов
docker stats gatsby-app-prod gatsby-postgres-prod

# Подключение к БД
docker exec -it gatsby-postgres-prod psql -U gatsby_user -d gatsby_db
```

---

**Дата применения:** _______________  
**Ответственный:** _______________  
**Результат:** [ ] Успешно  [ ] Требуется доработка  
**Комментарии:** ________________________________
